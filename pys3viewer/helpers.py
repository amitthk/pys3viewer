def get_files_in_bucket(bucketname):
    """All the files a bucket."""
    lstFiles = ['a.log', 'b.log', 'c.log'];
    return lstFiles;

def get_buckets_for_user(username):
    """All the buckets for a user."""
    lstBuckets = ['a', 'b', 'c'];
    return lstBuckets;